# 网络调试指南

[![English](https://img.shields.io/badge/Docs-English-blue.svg)](network_linux.md)

本文档介绍嵌入式系统开发中常用的网络调试命令和故障排查方法。

## 📋 目录

- [网络连通性测试](#网络连通性测试)
- [IP 冲突检测](#ip-冲突检测)
- [路由追踪](#路由追踪)
- [DNS 诊断](#dns-诊断)
- [网络接口配置](#网络接口配置)
- [端口检测](#端口检测)
- [抓包分析](#抓包分析)
- [网络故障排查流程](#网络故障排查流程)

---

## 网络连通性测试

### ping

`ping` 命令用于测试网络连通性，通过发送 ICMP 回显请求来检测目标主机是否可达。

#### 基本用法

```bash
# 基本 ping 测试
ping 192.168.1.1

# 指定次数
ping -c 4 192.168.1.1

# 持续 ping
ping 192.168.1.1

# 指定超时时间（秒）
ping -W 2 192.168.1.1

# 指定数据包大小（字节）
ping -s 1000 192.168.1.1

# 设置 TTL 值
ping -t 64 192.168.1.1

# 快速 ping（间隔 0.2 秒）
ping -i 0.2 192.168.1.1
```

#### 输出解读

```
PING 192.168.1.1 (192.168.1.1) 56(84) bytes of data.
64 bytes from 192.168.1.1: icmp_seq=1 ttl=64 time=0.523 ms
64 bytes from 192.168.1.1: icmp_seq=2 ttl=64 time=0.412 ms

--- 192.168.1.1 ping statistics ---
2 packets transmitted, 2 received, 0% packet loss, time 1001ms
rtt min/avg/max/mdev = 0.412/0.467/0.523/0.055 ms
```

| 字段 | 含义 |
|------|------|
| `icmp_seq` | ICMP 序列号 |
| `ttl` | 生存时间，每经过一个路由器减 1 |
| `time` | 往返时间（RTT） |
| `packet loss` | 丢包率 |
| `rtt min/avg/max` | 最小/平均/最大延迟 |

#### 常见问题诊断

| 现象 | 可能原因 |
|------|---------|
| `Destination Host Unreachable` | 目标主机不可达，检查网关或路由 |
| `Request timeout` | 超时，目标主机可能关闭或防火墙阻止 |
| 高丢包率 | 网络拥塞或链路不稳定 |
| 延迟波动大 | 网络质量差或存在干扰 |

---

## IP 冲突检测

### arp

`arp` 命令用于查看和管理 ARP 缓存表，可用于检测 IP 地址冲突。

#### 基本用法

```bash
# 查看 ARP 缓存表
arp -a

# 查看指定接口的 ARP 表（Linux）
arp -i eth0

# 删除 ARP 缓存条目
arp -d 192.168.1.100

# 手动添加静态 ARP 条目
arp -s 192.168.1.100 00:11:22:33:44:55
```

#### 使用 arping 检测 IP 冲突

```bash
# 检测 IP 是否冲突（Linux）
arping -D -I eth0 192.168.1.100

# 发送 ARP 请求
arping -c 3 192.168.1.1
```

#### 使用 ip neigh（现代 Linux）

```bash
# 查看 ARP 表
ip neigh show

# 删除条目
ip neigh del 192.168.1.100 dev eth0

# 添加静态条目
ip neigh add 192.168.1.100 lladdr 00:11:22:33:44:55 dev eth0
```

#### IP 冲突检测方法

1. **观察 ARP 表变化**
   ```bash
   # 清除 ARP 缓存后观察
   ip neigh flush all
   arp -a
   ```

2. **检查同一 IP 是否对应多个 MAC**
   ```bash
   arp -a | grep "192.168.1.100"
   ```

3. **使用 arping 主动探测**
   ```bash
   arping -D -c 3 -I eth0 192.168.1.100
   # 返回 0: 无冲突
   # 返回 1: 存在冲突
   ```

---

## 路由追踪

### traceroute

追踪数据包到达目标主机所经过的路由节点。

#### 基本用法

```bash
# 基本路由追踪
traceroute 8.8.8.8

# 使用 ICMP（需要 root 权限）
traceroute -I 8.8.8.8

# 使用 TCP
traceroute -T -p 80 8.8.8.8

# 指定最大跳数
traceroute -m 20 8.8.8.8

# 不解析域名（加快速度）
traceroute -n 8.8.8.8
```

#### 输出解读

```
traceroute to 8.8.8.8 (8.8.8.8), 30 hops max, 60 byte packets
 1  192.168.1.1 (192.168.1.1)  0.523 ms  0.412 ms  0.398 ms
 2  10.0.0.1 (10.0.0.1)  5.234 ms  5.123 ms  5.089 ms
 3  * * *
 4  8.8.8.8 (8.8.8.8)  20.123 ms  20.089 ms  20.045 ms
```

| 符号 | 含义 |
|------|------|
| `*` | 该跳无响应（可能被防火墙阻止） |
| 三个时间值 | 三次探测的往返时间 |

### mtr（推荐）

`mtr` 结合了 `ping` 和 `traceroute` 的功能，提供实时网络质量分析。

```bash
# 基本使用
mtr 8.8.8.8

# 报告模式
mtr -r -c 100 8.8.8.8

# 不解析域名
mtr -n 8.8.8.8
```

---

## DNS 诊断

### nslookup

```bash
# 基本域名解析
nslookup www.example.com

# 指定 DNS 服务器
nslookup www.example.com 8.8.8.8

# 查询特定记录类型
nslookup -type=MX example.com
nslookup -type=A example.com
nslookup -type=AAAA example.com
nslookup -type=CNAME example.com
```

### dig（推荐）

```bash
# 基本查询
dig www.example.com

# 简洁输出
dig +short www.example.com

# 指定 DNS 服务器
dig @8.8.8.8 www.example.com

# 查询特定记录
dig www.example.com A
dig www.example.com MX
dig www.example.com TXT

# 追踪解析过程
dig +trace www.example.com

# 反向解析
dig -x 8.8.8.8
```

### host

```bash
# 简单查询
host www.example.com

# 指定 DNS 服务器
host www.example.com 8.8.8.8

# 查询所有记录
host -a example.com
```

---

## 网络接口配置

### ifconfig（传统）

```bash
# 查看所有接口
ifconfig -a

# 查看指定接口
ifconfig eth0

# 配置 IP 地址
ifconfig eth0 192.168.1.100 netmask 255.255.255.0

# 启用/禁用接口
ifconfig eth0 up
ifconfig eth0 down

# 设置 MAC 地址
ifconfig eth0 hw ether 00:11:22:33:44:55
```

### ip（现代 Linux，推荐）

```bash
# 查看所有接口
ip addr show
ip a

# 查看指定接口
ip addr show eth0

# 配置 IP 地址
ip addr add 192.168.1.100/24 dev eth0

# 删除 IP 地址
ip addr del 192.168.1.100/24 dev eth0

# 启用/禁用接口
ip link set eth0 up
ip link set eth0 down

# 查看路由表
ip route show
ip r

# 添加默认网关
ip route add default via 192.168.1.1

# 添加静态路由
ip route add 10.0.0.0/8 via 192.168.1.1

# 查看链路状态
ip link show
```

### ethtool（网卡诊断）

```bash
# 查看网卡信息
ethtool eth0

# 查看链路状态
ethtool eth0 | grep "Link detected"

# 查看网卡统计
ethtool -S eth0

# 设置网卡速率
ethtool -s eth0 speed 100 duplex full
```

---

## 端口检测

### netstat

```bash
# 查看所有连接
netstat -a

# 查看监听端口
netstat -l

# 显示 TCP 连接
netstat -t

# 显示 UDP 连接
netstat -u

# 显示进程信息
netstat -p

# 不解析域名
netstat -n

# 常用组合：查看所有 TCP 监听端口
netstat -tlnp

# 查看特定端口
netstat -an | grep :80
```

### ss（现代 Linux，推荐）

```bash
# 查看所有连接
ss -a

# 查看监听端口
ss -l

# 查看 TCP 连接
ss -t

# 查看 UDP 连接
ss -u

# 显示进程信息
ss -p

# 常用组合
ss -tlnp

# 查看特定端口
ss -an | grep :80

# 查看连接状态统计
ss -s
```

### telnet / nc（端口连通性测试）

```bash
# 使用 telnet 测试端口
telnet 192.168.1.1 80

# 使用 nc（netcat）测试端口
nc -zv 192.168.1.1 80

# 扫描端口范围
nc -zv 192.168.1.1 20-100

# 测试 UDP 端口
nc -zuv 192.168.1.1 53
```

### lsof

```bash
# 查看端口占用
lsof -i :80

# 查看指定进程的网络连接
lsof -i -p 1234

# 查看所有网络连接
lsof -i
```

---

## 抓包分析

### tcpdump

```bash
# 抓取指定接口的数据包
tcpdump -i eth0

# 抓取指定主机的数据包
tcpdump host 192.168.1.100

# 抓取指定端口的数据包
tcpdump port 80

# 抓取指定协议
tcpdump icmp
tcpdump tcp
tcpdump udp

# 组合条件
tcpdump -i eth0 host 192.168.1.100 and port 80

# 保存到文件
tcpdump -i eth0 -w capture.pcap

# 读取抓包文件
tcpdump -r capture.pcap

# 显示详细信息
tcpdump -v
tcpdump -vv

# 显示数据包内容（十六进制）
tcpdump -X

# 不解析域名和端口
tcpdump -nn

# 限制抓包数量
tcpdump -c 100

# 常用组合
tcpdump -i eth0 -nn -X -c 100 host 192.168.1.100
```

### Wireshark（图形界面）

适用于桌面环境，支持更丰富的协议解析和过滤功能。

常用过滤表达式：

```
# IP 过滤
ip.addr == 192.168.1.100
ip.src == 192.168.1.100
ip.dst == 192.168.1.100

# 端口过滤
tcp.port == 80
udp.port == 53

# 协议过滤
http
dns
icmp

# 组合过滤
ip.addr == 192.168.1.100 && tcp.port == 80
```

---

## 网络故障排查流程

### 🔍 排查步骤

```
┌─────────────────────────────────────────────────────────┐
│                    网络故障排查流程                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 检查物理连接                                         │
│     └── 网线、指示灯、接口状态                            │
│              ↓                                          │
│  2. 检查本机网络配置                                      │
│     └── IP、子网掩码、网关、DNS                          │
│              ↓                                          │
│  3. 测试本地回环                                         │
│     └── ping 127.0.0.1                                 │
│              ↓                                          │
│  4. 测试网关连通性                                       │
│     └── ping 网关地址                                   │
│              ↓                                          │
│  5. 测试外网连通性                                       │
│     └── ping 8.8.8.8                                   │
│              ↓                                          │
│  6. 测试 DNS 解析                                       │
│     └── ping www.baidu.com                             │
│              ↓                                          │
│  7. 检查路由                                            │
│     └── traceroute 目标地址                             │
│              ↓                                          │
│  8. 抓包分析                                            │
│     └── tcpdump / Wireshark                            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 📋 常见问题排查表

| 问题现象 | 排查方向 | 常用命令 |
|---------|---------|---------|
| 无法 ping 通网关 | 检查 IP 配置、网线连接 | `ip addr`, `ethtool eth0` |
| 能 ping 通 IP 但无法 ping 通域名 | DNS 配置问题 | `cat /etc/resolv.conf`, `dig` |
| 网络时通时断 | 链路不稳定、IP 冲突 | `mtr`, `arping` |
| 连接超时 | 防火墙、端口未开放 | `iptables -L`, `ss -tlnp` |
| 网速慢 | 带宽限制、网络拥塞 | `iperf3`, `ethtool -S` |
| 无法访问特定端口 | 服务未启动、防火墙 | `ss -tlnp`, `telnet` |

### 🛠️ 快速诊断脚本

```bash
#!/bin/bash
# network_check.sh - 网络快速诊断脚本

echo "=== 网络接口状态 ==="
ip addr show

echo -e "\n=== 路由表 ==="
ip route show

echo -e "\n=== DNS 配置 ==="
cat /etc/resolv.conf

echo -e "\n=== 测试本地回环 ==="
ping -c 2 127.0.0.1

echo -e "\n=== 测试网关 ==="
GATEWAY=$(ip route | grep default | awk '{print $3}')
ping -c 2 $GATEWAY

echo -e "\n=== 测试外网 ==="
ping -c 2 8.8.8.8

echo -e "\n=== 测试 DNS 解析 ==="
ping -c 2 www.baidu.com

echo -e "\n=== 监听端口 ==="
ss -tlnp
```

---

## 嵌入式网络调试注意事项

### 资源受限环境

在嵌入式系统中，某些命令可能不可用或功能受限：

| 标准命令 | 嵌入式替代方案 |
|---------|---------------|
| `ifconfig` | `ip` 或 BusyBox `ifconfig` |
| `netstat` | `ss` 或 `/proc/net/*` |
| `traceroute` | BusyBox `traceroute` |
| `tcpdump` | 精简版 `tcpdump` 或 `dumpcap` |

### 查看 /proc 网络信息

```bash
# 网络接口统计
cat /proc/net/dev

# TCP 连接状态
cat /proc/net/tcp

# UDP 连接状态
cat /proc/net/udp

# ARP 表
cat /proc/net/arp

# 路由表
cat /proc/net/route
```

### 常用网络配置文件

```bash
# 网络接口配置（Debian/Ubuntu）
/etc/network/interfaces

# DNS 配置
/etc/resolv.conf

# 主机名解析
/etc/hosts

# 网络服务端口
/etc/services
```

---

<p align="center">
  <a href="README_zh.md">返回主页</a>
</p>
